<!-- ============================================================
     servo_x.xml — PAN(yaw)サーボ単体 (MG996R)
     px3_servo_node.h の SERVO_X_PIN = 10 に対応。
     土台に接着固定される側。出力軸(z軸)より上が可動部の起点。
     寸法: 40.7 x 19.7 x 42.9mm (データシート確定値)
     質量: 55g
     ============================================================ -->
<mujocoinclude>
  <body name="servo_x_body" pos="0 0 0.0215">
    <geom name="servo_x_geom" type="box"
          size="0.0204 0.0099 0.0215"
          mass="0.055" rgba="0.2 0.2 0.25 1"/>
  </body>
</mujocoinclude>
